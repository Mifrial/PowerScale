/**
 * Семья оружия — группа владения «Владения оружием» (таблица «Навыки владения» карточки оружия).
 * Семья несёт единственную лестницу стоимостей уровней владения; оружие ссылается на неё
 * (`ItemSpecBase.proficiency_family_code`). Домен способности «Владение оружием» = правила weapon_family.
 */
export interface WeaponFamilySpec {
  /** Лестница стоимостей уровней владения семьёй («1, 3, 5»). */
  costs: number[];
}
