/** Спек группирующего правила (type 'group'): контейнер способностей с лимитом выбора. */
export interface GroupSpec {
  type: 'group';
  /** Сколько способностей из группы можно выбрать (1 = «1 из группы», N>1 = «не более N», -1 = без лимита). */
  selectLimit: number;
}
