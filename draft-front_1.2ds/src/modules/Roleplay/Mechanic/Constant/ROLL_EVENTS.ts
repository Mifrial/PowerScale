/**
 * События потока броска (порядок): `pool` → бросок → `drop` → базовый подсчёт → `score` → итог.
 * Правила с механиками подписывают хендлеры на эти события (см. RollMechanicContext).
 */
export const ROLL_EVENTS = {
  /** До броска: добавить кубы в пул (Помехи/Преимущества). */
  pool: 'roll.pool',
  /** После броска, перед подсчётом: убрать худшие/лучшие кубы (Помехи/Преимущества). */
  drop: 'roll.drop',
  /** После подсчёта успехов: начислить/списать успехи за значения кубов («6 и 1» и др.). */
  score: 'roll.score',
} as const;
