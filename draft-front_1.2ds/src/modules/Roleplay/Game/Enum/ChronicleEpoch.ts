/**
 * Точка отсчёта летописи (ТР §8 «Летопись»). Задел на будущее: поле эпохи в модели,
 * сейчас — единственное значение «от начала приключения» (старт игры).
 */
export type ChronicleEpoch = 'adventure_start';
