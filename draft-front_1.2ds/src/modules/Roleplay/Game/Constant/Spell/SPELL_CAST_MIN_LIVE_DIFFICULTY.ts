import type { DimensionalNumberValue } from '@/modules/Core/Engine/Dto/DimensionalNumberValue';

/** Нижняя живая сложность: ниже — проверка не нужна. */
export const SPELL_CAST_MIN_LIVE_DIFFICULTY: DimensionalNumberValue = { base: 3, size: -1 };
