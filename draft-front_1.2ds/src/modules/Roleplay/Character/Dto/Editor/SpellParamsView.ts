/** Подписи параметров заклинания в развёрнутой строке редактора персонажа. */
export interface SpellParamsView {
  powerLabel: string;
  controlLabel: string;
  creationLabel: string | null;
  durationLabel: string;
  componentsLabel: string | null;
}
