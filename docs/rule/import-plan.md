# Импорт правил из docx — план доработок

Живой документ. Источник и маппинг — `docs/rule/import-context.md`. Контекст-файл и этот план держим в актуальном состоянии; идём по шагам сверху вниз. Каждый шаг = отдельная доработка с детальным разбором, гейтом и фиксацией решений.

Легенда статуса: 🔵 не начат · 🟡 в работе · 🟢 готов · ⚪ отложен/заморожен.

## Фаза 0 — Основа процесса

| Шаг | Что делаем | Модель/изменения | Источник | Гейт | Статус |
|-----|------------|------------------|----------|------|--------|
| S0.1 | Создать `docs/rule/import-context.md` | — | — | файл на месте, маппинг зафиксирован | 🟢 |
| S0.2 | Создать этот файл `docs/rule/import-plan.md` | — | — | файл на месте | 🟢 |
| S0.3 | Узел memory-графа (work_item) со ссылками | — | — | узел создан | 🟢 |

## Фаза 1 — ОС-черты «Основа» (замена тестового набора способностей)

| Шаг | Что делаем | Модель/изменения | Источник | Гейт | Статус |
|-----|------------|------------------|----------|------|--------|
| S1 | Инвентарь ОС-черт из докла: тип/зона os/цена/дар/признаки/группа; разбор шкал-выборов (Зрение/Слух/Внешность/Выносливость/Умственные) и «черт вида» | решения №8–12; дискуссии 1(Восприятие),2(параметры X) | §Уникальные, §Вид, §Общие | чек-лист заполнен; решения+дискуссии в контексте | 🟢 |
| S3 | Модельные решения: шкала-выбор и запрет совмещения (**группа**); `characteristic_modify` на базу производной; grant `resistance`; параметры `{code}` + резолв; **каркас механик** (реестр `Mechanic`+версии, `mechanic_payload` на Rule, реестр хендлеров `code@version` со scope, диспетчер по `step`, хендлер `purchase_surcharge`) | Requirement/Grant/AbilitySpec + механики (реестр/хендлеры) + дизайн-спеки | §3, §Общие, №8/№10/№11/№12, Д4 | решение зафикс. в контексте, код покрыт тестом | 🟢 |
| S2 | Новый `Rule/Mock/mockRuleImport.ts` + врезка в `ruleCatalog` (unique id/code); данные ложатся на готовую модель S3 | только данные | см. S1 + S3 | vitest + lint + vue-tsc; карточки черт рендерятся | 🟢 |
| S4 | Подключить набор в этап «Основа» (`BaseTab`), проверить фильтры/цены/уровни | UI | — | headless/CDP, визуальный чек | 🟢 |

## Фаза 2 — Профили видов/рас

| Шаг | Что делаем | Модель/изменения | Источник | Гейт | Статус |
|-----|------------|------------------|----------|------|--------|
| S5 | Разбор видов/рас, маппинг характеристик (Выносливость→Стойкость; Восприятие/Интеллект=min), спец-виды (Конструкт/Фея) отложить или пометить | — (анализ) | §Человек…§Дворф, §Орк | чек-лист заполнен | 🟢 |
| S6 | Карты `species/race`: cost_os (в т.ч. отриц.), характеристики fixed/purchased, automatic-способности. **human → species** (вид); расы-дети. Объём: **Человек×9, Эльфы (Верто→Арилет, Литен, Труул), Орк×4, Дворф (Турим) ✅** | данные | см. S5 | vitest/lint/tsc, RaceTab рендерит | 🟢 |
| S7 | Подключить в «Раса», проверить дерево вид→подвид→раса, наследование | UI | — | headless/CDP | 🟢 |

## Фаза 3 — Таблицы характеристик

| Шаг | Что делаем | Модель/изменения | Источник | Гейт | Статус |
|-----|------------|------------------|----------|------|--------|
| S8 | Глобальная «Телосложение» (модификатор→цена −3..+8) + ограничения (Сила ≤ Стойкость+3; Восприятие/Интеллект ≤ +1). **Форма решена: черты «Врождённая X»** | Rule/Race/валидация | §Черты характеристик | решение в контексте, тест | 🟢 |
| S9 | ~~Таблица «Магия Х» (размерная цена 3↓…5↑), «Магия как черта с потолком Х»~~ — **готово**: «Врождённая Магия X» (дар, parameter_table, размерные ключи 3↓..5↑ → 1..20 ОС из дока); перенос черт характеристик на вкладку «Характеристики» | Ability/Resource | §Черты характеристик | тест + UI | 🟢 |
| S10 | Валидация ограничений на этапе «Характеристики» + подсказки в UI | UI + валидация | — | headless/CDP | 🟢 |

## Фаза 4 — Личность (ОЛ)

| Шаг | Что делаем | Модель/изменения | Источник | Гейт | Статус |
|-----|------------|------------------|----------|------|--------|
| S11 | 15 особенностей как `ol`-особей (отрицательные стоимости; «даёт навык»; кардинальность групп Общение/Богатство; признак «Богатство» — не в лимите числа особенностей, взятие только при создании; грант `money` fixed/percent от лимита денег игры). **+ возраст**: тип правила `age` (`Age {name, ol, featureLimit, effects}`) + таблицы лет у рас (`{age, ageStart, ageEnd}`, ступень «Старый» за диапазонами) | данные + модель (тип `age`, грант `money`) | §Личность, `docs/rule/age/` | тест | 🟢 |
| S12 | Этап «Личность» вместо заглушки: секция возраста (годы → ступень → ОЛ/лимит, live-эффекты), особенности зоны `ol` по образцу BaseTab, лимит числа особенностей (без «Богатства»), «ОЛ сгорают»; шаг скрывается при отсутствии `ol` | UI/Store | — | headless/CDP | 🟢 |

## Фаза 5 — Спец-механики (кандидат-опции, решаем осознанно)

| Шаг | Что делаем | Модель/изменения | Источник | Гейт | Статус |
|-----|------------|------------------|----------|------|--------|
| S13 | Энергетика конструктов/заряд, ядро монстра/постоянная магия/эволюция, возрастные ориентиры | модель (большая) | §Конструкт, §Черты монстра | решение: берём/откладываем | 🔵 |

## Фаза 6 — Развитие (ОР), заход «Развитие» (согласован 2026-08-09)

> План и решения: `docs/specs/development-module-context.md` (D103–D113). Каталог: `docs/rule/skills/catalog.md` (из `docs/rule/skills/AI.html`). Терминология остаётся мок (Проворство→Ловкость, Выносливость→Стойкость, Мастерство общения→Общение).

| Шаг | Что делаем | Модель/изменения | Источник | Гейт | Статус |
|-----|------------|------------------|----------|------|--------|
| S14 | Каталог навыков «Развития» (зона `or`) в мок: 194 карточки, 11 разделов; типы без новых AbilityType (Реакция/Манёвр — признак действия; Множественный — `multiple` + домен-справочник/кастом; Техника — признак; Эффект → `state`); стоимости array (поуровневые) и `progression` («Стоимость N, Трудность M»); улучшения с требованиями; характеристики Мелкая моторика/Музицирование (Телосложение=Стойкость); «Черты развития»/«Счётный разум»/«Клич» удалены из выгрузки | данные + модель (флаг `multiple`, домен, `zone` покупки, требования с размерами) | `docs/rule/skills/catalog.md` | vitest + lint + vue-tsc | 🟢 |
| S15 | Этап «Развитие» вместо заглушки: каталог зоны `or` (фильтры раздел/тип/признак/доступность, покупка уровней, требования, улучшения), бюджет «ОР: N / N», агрегаты «Развитие X» (авто-бонус), производный «Ближний бой» (уровень из суммы стоимостей), дары-навыки от особенностей (D100); боевые — описательно | UI/Store/механики | — | headless/CDP | 🟢 |

## Фаза 7 — Инвентарь (предметы/закупка), заход «Инвентарь» (согласован 2026-08-11)

> План и решения: `docs/specs/inventory-module-context.md` (R1–R26, семантика «Силы удара», курс валют). Каталог: `docs/rule/battle/catalog.md` (90 карточек из `docs/rule/battle/AI.html`). Скоуп: всё (снаряжение + артефакты/зелья + прайсы товаров); оружейные навыки и модификаторы — отложены.

| Шаг | Что делаем | Модель/изменения | Источник | Гейт | Статус |
|-----|------------|------------------|----------|------|--------|
| S16 | Каталог предметов в мок: ~90 правил `type: 'item'` (оружие/щиты/доспехи/артефакты/зелья/капли/бомба/товары/кристаллы); профили атак (`actionCharacteristic`-формулы, `action_characteristics`, `falloff`), `proficiency` (группа/лестница/схожее), признаки-разделы; `ArmorBlock.max_agility/strength_penalty`, `WeaponBlock/ShieldBlock.durability`, `group_code` | данные + модель (Formula `actionCharacteristic`, `ActionCharacteristicModifier`, `ActionCharacteristicValue`) | `docs/rule/battle/catalog.md` | vitest + lint + vue-tsc | 🟢 |
| S17 | Шаг «Инвентарь» вместо заглушки: каталог (FilterBar + VirtualList + разделы), покупка/отмена (базовая линия), экипировка (тумблер), деньги (гм + размен), навигация | UI/Store/сервисы | — | headless/CDP | 🔵 |
| S18 | Статы мастерства + «Владение оружием» (множественная способность, домен = группа оружия, per-weapon лестницы, гранты и производные тайлы) | модель/UI | — | тест | 🟡 (статы: `melee-combat`→«Мастерство ближнего боя», новая `ranged-combat`«Мастерство дальнего боя» auto 3 — готово; владение оружием — в работе) |

## Решения по фазам (журнал)

- 2026-08-07 · Маппинг характеристик (Выносливость→Стойкость; Восприятие/Интеллект=min; бонус к производной → бонус к базе) — зафиксирован в контексте.
- 2026-08-07 · Прогрессивную доплату +2 ОС берём; механику обсуждаем на S3.
- 2026-08-07 · Шкалы Зрение/Слух/… первонач. описаны «уровнями», но заменено решением №8 («отдельные способности + «1 из группы»») — «уровни» устарели.
- 2026-08-07 · Группировка шкал-выборов (№8): отдельные способности + ограничение «1 из группы» (группа/флаг в AbilitySpec), НЕ уровни одной способности.
- 2026-08-07 · Порядок S2/S3 перевернут: S3 (модель) раньше S2 (данные). Внутри S3 приоритет: сначала группа (`group`+«1 из группы»), затем параметры, resistance.
- 2026-08-07 · Дискуссия 4 решена: каркас механик (реестр `Mechanic`+версии, `mechanic_payload` на Rule, хендлеры `code@version` со scope, диспетчер по step, чистые механики `payload+read-state → {effects, ui}`, категории активные/пассивные, хендлер `purchase_surcharge` для «Общее»/«Зверолюди»). В контексте.
- 2026-08-07 · Черты «Слабая/Сильная выносливость» **исключены из выгрузки** — вместе с ними отпал `spent_on_category`.
- 2026-08-07 · **Состав S2 принят**: Быстроногий, Толстые пальцы, Внешность×4, Голос×2, Слух×5, Зрение×5, Умственные×2, Устрашающий вид, **Сопротивление магии X** (параметрическая 2x + resistance-грант); механика «Общие» (purchase_surcharge). Отложены: спеллы, монстр, Энергохранилище X, Однорукий, Зверолюди.
- 2026-08-07 · **Параметры + resistance-грант делаем в этом заходе** (S3) — нужны для «Сопротивления магии X» в S2.
- 2026-08-07 · **S3 готов**: каркас механик (реестр `Mechanic`+версии, `mechanic_payload` на Rule, реестр хендлеров `code@version` со scope `character.edit.osSteps`, диспетчер по step, хендлер `purchase_surcharge`); параметры (`AbilityParameter` + `parameters` на AbilitySpecBase + `AbilityCost.kind='parameter'` + `CharacterAbility.parameters`); grant `resistance`; группа (первично `group {groupCode, selectLimit}`, **в S7 переработано в правило-контейнер type `group`**) + кардинальность в `setAbilityLevel`. Тесты: mechanicDispatcher, characterEditor (параметрическая цена), characterBuild (группа), ruleValidation (resistance).
- 2026-08-07 · **S2 готов**: `mockRuleImport.ts` врезан в `ruleCatalog`. Набор: Быстроногий, Толстые пальцы, Сопротивление магии X (параметр x ×2 + resistance magic-damage), Внешность×4, Голос×2, Слух×5, Зрение×5, Умственные×2, Устрашающий вид (or has_ability), тип урона `magic-damage`, механика «Общие» (purchase_surcharge, keyword common). Признак keyword `common` (id 20). Тест: mockRuleImport (8 кейсов).
- 2026-08-07 · **S4 готов**: механики прокинуты в редактор — `build()`/`toVersion()` принимают `mechanics`; `buildBudgets` прогоняет диспетчер по scope `character.edit.osSteps` и сворачивает эффекты в `os.spent` (доплата +2 ОС за 3-ю+ общую черту). `CharacterReferenceService.rules()`, загрузка механик в `CharacterEditPage`. Тест: characterEditor (механика в бюджете). Build vite ✓.
- 2026-08-07 · **S5 готов**: инвентарь видов/рас (Человек×9, Эльфы, Дворф, Орк×4, Конструкт, Фея). Птицелюд/Зверолюд в тексте источника отсутствуют — не берём. Конструкт/Фея — спец-виды → Фаза 5. Модификаторы «от тела» = покупные характеристики (mode purchased).
- 2026-08-07 · **S6 готов**: `human` → species; `mockRaceImport.ts` — человеческие расы×9 + Эльфы (species Верто → раса Арилет; расы Литен, Труул; parent wood-elves), Дворфы (species dwarves → раса Турим), Орки (species orcs → Оргул/Орхан/Орзак/Му’укай). Характеристики: база 3 + переопределения, размерные значения; признаки одноимённые; видимость черт (common/racial/выбранные). У эльфов в доке «Стойкость 5↓, Выносливость 5» — оба → endurance по №1, берём Выносливость. Турим cost_os ? → 0. Расходные черты (Пиворождённый, Темновидение и пр.) не в каталоге — отложено.
- 2026-08-07 · **S7 готов**: RaceTab строит дерево вид→подвид→раса (species expandable, race selectable); наследование способностей по цепочке (Арилет → Верто → Лесной эльф → Эльфы: keen-hearing). Тест: characterEditorCatalog «наследование» + «дерево рас». Build vite ✓.
- 2026-08-07 · **Расы приведены к формату**: удалены старые тестовые расы `elf` (rule-29) и `wood-elf` (rule-30); виды `elves`/`wood-elves` приведены к корректным признакам ([гуманоид, эльф]/[гуманоид, эльф, лесной эльф]). Наполнены abilities рас: бесплатные (automatic: true) и приобретаемые (automatic: false) — Дюариец (Сопротивление холоду, Магия, Сопротивление магии), Алиерц/Анеит/Ахтар/Орф/Ацелатль/Иерит/Аэрон (Магия, Сопротивление магии), эльфы (Магия), Турим (Пиворождённый/Маленький шаг/Темновидение/Магия), орки (Большой/Быстроногий/Сопротивление магии/Магия/Видящий/Чующий). Созданы черты-заглушки: cold-resistance, dark-vision, beerborn, small-step, big-build, seeing, scenting, magic-potential + тип урона cold. **Расовый automatic теперь даёт бесплатность** (`EditorAbility.automatic` учитывает расовые refs automatic: true).
- 2026-08-07 · **Группы переделаны в правила-контейнеры (тип `group`)**: у группы имя/описание/признак «Группа»/`selectLimit` (1 = «1 из группы», N>1 = «не более N», -1 = без лимита); участники — `group_code` + признак «Часть группы». `AbilityType` + `group`, новый `GroupSpec`, `EditorAbilityGroup` в модели редактора; редактор персонажа рендерит способности строго внутри групп (заголовок + счётчик «выбрано X из N»), кардинальность в `setAbilityLevel` (снятие остальных при 1, отклонение добавления при N>1). Данные: 5 групповых правил (appearance/voice/hearing/vision/mental, id 106–110), id рас сдвинуты на +5 (arilet 116→121, liten 117→122, … muukai 125→130). Тесты: mockRuleImport (группы), characterBuild (1 из группы + N>1), characterEditorCatalog (groups в модели).
- 2026-08-07 · **Потолок «X» у расы + покупка параметрических способностей (S8)**: `RaceAbilityRef.parameters` (потолок для доступных, значение для `automatic`); в модели редактора `EditorAbility.parameters` (value/min/max с учётом потолка расы, perUnit, cappedByRace); способность берётся степпером «−/+/снять» (цена `per_unit × X`); автоматические расовые способности применяют дары (уровень 1); масштабирование даров по X — формула `{type:'parameter', per_unit}` (`Grant.resistance.value = DimensionalNumberValue | Formula`, резолв в `forEachActiveGrant`). Имена с потолком: «Сопротивление магии X» → «Сопротивление магии до 2» (RaceTab/RaceCard/Основа). Редактор расы — поле «Потолок X» в RaceAbilitiesEditor. Данные: Дюариец `magic-resistance {x:2}`, `magic-potential {x:4}`; Ахтар auto `magic-resistance {x:2}`, `magic-potential {x:5}`; остальные по комментариям. Тесты: formulaEvaluation (parameter), characterEditor (авто-дары), characterBuild (setAbilityParameter), characterEditorCatalog (потолок/авто-значение).
- 2026-08-08 · **Чувства + размерный перенос модификаторов характеристик (баг «Внимательность 9»)**: (а) значение характеристики считается `modify(delta, CHARACTERISTIC_BASE_RANGE)`, а не линейно `base + delta` (редактор `buildCharacteristics`, Обзор `buildCharacteristicCore`/melee) — «Невероятное зрение» +6 на базе 3 → `{3|2}`, а не 9. (б) Чувство = тип правила `sense` + грант `sense_modify` (от источника «Совершенство»); черты Слуха/Зрения теперь дают модификатор чувству, а не Внимательности напрямую. Инстанс `CharacterSenseValue { ruleId, value, modifiers }` в модели редактора и `CharacterVersion.senses`; значение чувства агрегируется по общему правилу источников (у одного источника макс+/макс−), чувство без грантов = 0; **Внимательность берёт максимум среди чувств** как модификатор «от {чувство}» (роль «от чувства») и складывает с обычными модификаторами. UI: блок «Чувства» в попапе Внимательность (значения чувств). Данные: правила `sense-hearing`/`sense-vision` (id 132/133), source `perfection` (id 131). Тесты: mockRuleImport (sense_modify), characterEditorCatalog (макс среди чувств, −9+норма→0, `{3|2}`), DimensionalNumber/editor (размерный перенос).
- 2026-08-08 · **Параметры рас доведены до дока + докупка сверх авто-значения**: у всех рас заполнены `parameters: x(N)` параметрических способностей по `docs/rule/AI.html` — Дюариец `cold-resistance auto {x:1}`, Аэрон `magic-resistance auto {x:2}` + доступная `{x:5}` + `magic-potential {x:3}`, Арилет/Литен/Труул `magic-resistance auto {x:1}` + доступная `{x:3}` + `magic-potential {x:4}`, Турим `magic-potential {x:5}`, орки `magic-resistance auto {x:1/2/3}` + `magic-potential {x:3}`, Му’укай `magic-resistance auto {x:1}` и **убрана** `magic-potential` (в доке нет). Механика: `racialAutomaticValues` (бесплатное авто-значение) отдельно от потолков; `EditorAbilityParameter.freeValue`; автоматический грант применяется только если способности нет в `build.abilities` (иначе задвоение при докупке); стоимость докупки = `per_unit × (X − авто)`; `cappedByRace` = «потолок расы задан» (имя «до N» показывается всегда при заданных parameters); степперы редактора: для авто-способности можно докупить до потолка. Тесты: mockRaceImport (parameters всех рас), characterEditorCatalog (авто-значения орков/эльфов/аэрона, потолки, Аэрон докупка x5 = 12+6 ОС, Му’укай без Магии), characterBuild (setAbilityParameter).
- 2026-08-08 · **S8 «Телосложение» = черты «Врождённая X» (форма решена)**: 5 общих черт `innate-strength/endurance/dexterity/intellect/perception` («Врождённая Сила X» и т.д.) — модификатор характеристики от тела по **таблице цен** дока (модификатор −3/−2/−1/+1/+2/+3 → −3/−2/−1/2/4/8 ОС; отрицательные возвращают ОС). Модель: новый `AbilityCost.kind 'parameter_table'` (`costs: Record<string, number>`, стоимость = costs[X]); параметр `x` с min −3/max +3/default 0 (X=0 = не взята, X<0 допустим — `setAbilityParameter` сохраняет отрицательные). Грант `characteristic_modify` с формулой параметра (резолв в `resolveGrant` → fixed). **Связь Сила↔Стойкость** (`AbilityParameter.linked`: ability_code/parameter_code/max_delta): диапазон параметра сжимается до |значение связанного ± max_delta| в `editorParameters` (Сила X=2 → Стойкость от −1 до 3). UI: степперы поддерживают отрицательный диапазон и табличную цену шага (`costs`). Тесты: mockRuleImport (parameter_table + linked), characterEditorCatalog (стоимость по таблице, −1 возвращает ОС, связь Стойкости от Силы), characterBuild (отрицательный X). id рас сдвинуты +5 (nextId 111→116) — mockRuleImport дорос до 115.
- 2026-08-08 · **S9 «Врождённая Магия X» + перенос черт характеристик на вкладку «Характеристики»**: «Магия X» переименована в «Врождённая Магия X» и переведена с `parameter` (per_unit 1) на **`parameter_table` с размерными значениями** (дар): costs-ключи — toString() размерных чисел (`'3↓':1, '4↓':2, '5↓':3, '3':4, '4':6, '5':8, '3↑':12, '4↑':16, '5↑':20`), параметр `x` min {3,−1}/max {5,1}/default {3,0} (значение-потолок). Признаки: «Врождённая» (44), «Характеристика» (45), «Модификатор» (46) у врождённых ±, «Дар» (47) у Магии. `EditorAbility.characteristic`/`characteristicCode` (из гранта characteristic_modify); `EditorAbilityParameter.steps` — лестница значений (из costs через `DimensionalNumber.parse`); `setAbilityParameter` принимает размерное значение; Восприятие/Интеллект ограничены **±1** (док: макс. модификатор от Телосложения +1). **Перенос**: черты характеристик исключены из каталога «Основа» (BaseTab фильтрует `ability.characteristic`), на вкладке «Характеристики» — блок `EditorInnateMatrix` (матрица «Покупка»: строки=характеристики, столбцы=значения X, ячейки=цена ОС, колонка «Итог»; две секции «Модификаторы» и «Дар»). Пустая «Покупка характеристик (ОС)» скрывается. Тесты: mockRuleImport (признаки, Магия parameter_table, ±1), characterEditorCatalog (steps Магии, выбор 5→8 ОС, characteristic-флаг), characterBuild (размерное значение), DimensionalNumber.parse. **Фиксы багов**: новый грант `characteristic_parameter` (база характеристики = X) — приобретённая Магия отображается среди характеристик/в общем блоке; подзаголовок шага «Характеристики» считает ОС покупок + врождённых черт (показывал 0).
- 2026-08-09 · **Бесплатная база расы + инкрементальные цены дара (Ахтар/Анеит, баги «Врождённой Магии»)**: раса с фикс. характеристикой (`mode 'fixed'`, напр. «Магия 4↓») даёт дару (грант `characteristic_parameter`) бесплатную ступень: `editorParameters` ставит `value`/`min` на фикс. базу (ступени ниже недоступны), `freeStepCost` = табличная цена базы (`costs[toString()]`), стоимости ступеней и зоны инкрементальны (`табл(X) − табл(базы)`: для Ахтара 5↓=1, 3=2, 4=4, 5=6; потолок 5). `parameterCostValue` для `parameter_table` теперь возвращает число (цена минус бесплатная ступень) вместо строки-ключа; `EditorAbilityParameter.freeStepCost`; матрица `EditorInnateMatrix.costAt` вычитает её. **Баг «покупка не меняет характеристику»**: грант `characteristic` переопределяет расовую fixed-базу (убран ранний `return`, если база уже есть). Бесплатная база — только для даров (`characteristic_parameter`), НЕ для `characteristic_modify`. Данные рас не менялись.
- 2026-08-09 · **Сравнения размерных чисел + `CharacteristicNumber`**: в `DimensionalNumber` добавлены `equalsStrict` (base+size), `equals` (нестрогое: больший размер приводится к меньшему, сравнение баз) и `compare` (−1/0/1). `toNumber()` остаётся только для цен/отображения, НЕ для сравнений (floor скрывает разницу `{5↓}`=2.5 vs `{4↓}`=2). Новый `Rule/Value/CharacteristicNumber` (extends DimensionalNumber): `modifyWith(delta)` = `modify(delta, RANGE)`, `modifyDiffTo(other)` = разность в пунктах `(base + step·size)`, step=3 (обратна `modifyWith`); `CHARACTERISTIC_BASE_RANGE` перенесён в Rule, на старом месте — re-export. `evaluateDerivedValue` и `raceCharacteristicLabels` сравнивают по значению (`compare`/`equals`), а не по `toNumber`. Миграция FormulaEvaluation/Overview на CharacteristicNumber — отдельным шагом.
- 2026-08-09 · **Заход C (Личность + возраст) согласован**: тип правила `age` — `Age {name, ol, featureLimit, effects}`, у расы таблица `{age, ageStart, ageEnd}` (наследуется по parent_race_code), разрешение — первый диапазон `ageStart ≤ годы < ageEnd`, за диапазонами — ступень «Старый» (не ограничено); эффекты возраста — real-модификаторы с `scope` (условные — scope; «восстановление ×2/×3» — описанием), live. Деньги: базовый капитал = лимит денег игры (`moneyBudget`, дефолт 10000 гм); Обеспеченный `max(+50, 50%)`, Преуспевающий `max(+100, 100%)`, Богатый `max(+400, 400%)`, Нищий `min(10%, 10 гз)`; при `moneyBudget = null` — только fixed. Признак «Богатство» — особенности с ним не учитываются в лимите числа особенностей; взятие только при создании. План и решения: `docs/specs/character-module-context.md` §4.8.
- 2026-08-09 · **S11/S12 готовы (заход C реализован)**: тип правила `age` (`AgeSpec`), `SpeciesSpec.age_years`, `CharacterBuild.ageYears`/`CharacterVersion.ageYears`, ОЛ = ступень возраста; 15 ol-особенностей (зона ol, отрицательные стоимости, группы sociability/attentiveness/wealth, признак wealth, грант `money`, 8 навыков для «даёт навык»); `PersonalityTab` (выбор возраста, лимит числа особенностей, «ОЛ сгорают»), шаг «Личность» скрыт без правила age; богатство — только при создании. Гейт: vitest 473/473, vue-tsc, lint, vite build ✓.
- 2026-08-09 · **Заход «Развитие» (ОР) согласован (S14/S15)**: каталог навыков из `docs/rule/skills/AI.html` (нормализован в `catalog.md`). Ключевые решения (D103–D113 в `docs/specs/development-module-context.md`): новые AbilityType НЕ вводим (Реакция/Манёвр — признак действия; Множественный — `skill` + флаг `multiple` + домен-справочник/кастом; Техника метания — признак; Клич — не делаем; Эффект — `state`); «Черты развития»/«Счётный разум»/«ОР Интеллекта» — удалены из каталога (случайные карточки выгрузки); характеристики Мелкая моторика/Музицирование добавляем (Телосложение=Стойкость, Внешности нет); зона покупки — `zone` в записи способности (мультизона os+or перестаёт задваивать ОР); требования с размерами — парсим через `CharacteristicNumber`; агрегаты «Развитие X» и производный «Ближний бой» — вычисляем; боевые способности — описательно (без симуляции); владение оружием — отложено.
- 2026-08-09 · **S14/S15 готовы (заход «Развитие» реализован)**: `mockDevelopmentImport.ts` — 194 правила (11 разделов, costs array/progression «Стоимость N, Трудность M», множественные с домен-справочником, требования с размерами через `CharacteristicNumber`); `AbilitySpecBase.multiple/domain_ref/aggregate/derived_level`; зона покупки (`CharacterBuild.abilities[i].zone`, списывает только её); характеристики Мелкая моторика (rule-174) и Музицирование (rule-175); `DevelopmentTab.vue` (каталог or: фильтры раздел/тип/доступность, покупка, бюджет ОР, дары-навыки, домен множественных); агрегаты «Развитие X» (авто-бонус к базам производной) и производный «Ближний бой» (уровень из суммы стоимостей, требование-ссылки работают); дары-навыки от особенностей (грант `ability` + `level`, поле `EditorAbility.gifted`, видны как «получено от особенности», учитываются в `has_ability`). Гейт: vitest 489/489, vue-tsc, lint, vite build ✓; headless-CDP: каталог 194, покупка «Ориентирования» ОР 0→2.
- 2026-08-09 · **Отладка UI «Развития»**: (1) убрана шапка «Навыки развития [ОР: N/N]» — бюджет уже в навигации шага (EditorStageNav, подзаголовок «Развитие»), дары — в карточке ниже; (2) «Раздел» перенесён из отдельного v-select в фильтр-бар (`FilterField {key:'section', type:'select', options: SECTIONS}`; `devAbilities` дополняется полем `section`; фильтрует через `useFilteredRows` select-predicate). Проверено CDP: попап фильтра — «Название» + «Раздел» (11 опций), выбор «Тело» → «Все навыки (17)». Гейт: vitest 489/489, vue-tsc, lint, vite build ✓.
- 2026-08-09 · **Производительность «Развития» — анализ (замеры headless-CDP)**: монтаж редактора ~2.4с (все вкладки смонтированы сразу, VWindowItem v-show); переключение на вкладку ~2.6с (раскрытие скрытого поддерева ~5.5k DOM-узлов, 196 панелей, 504 кнопки); разворот «Все навыки» — JS-задача ~2.9с (VExpansionPanelText лениво монтирует 194 Vuetify-панели), сворачивание ~2.0с (демонтаж). Причины: 194 тяжёлых v-expansion-panel; ленивое монтирование контента при каждом развороте/свороте; вкладки не монтируются лениво (v-show). План исправления — см. `docs/specs/development-module-context.md` (раздел про производительность).
- 2026-08-09 · **Виртуализация каталога «Развития» реализована** (решение B, шаги 0–3): переиспользуемый `VirtualList.vue` (Core, `@tanstack/vue-virtual`, дженерик T, динамические высоты через `measureElement`+ResizeObserver, `resetKey` сброс скролла, слот `{item,index}`); `EditorAbilityRow.vue` — одиночная панель с управляемым раскрытием (`update:open`, Set<ruleId> переживает ремаунты), `EditorAbilityPanels.vue` — обёртка (BaseTab/PersonalityTab/GroupPanels не меняются); `DevelopmentTab`: аккордеон «Все навыки» → статичный заголовок + VirtualList (высота `calc(100vh - 260px)`), быстрый фильтр сокращён до (Все / Атаки / Физическое развитие), «Приобретённые» чип рядом с «Доступные» (level>0 || gifted || automatic), аккордеон «Выбранные навыки» удалён; `RuleListPanel` (деталка пространства) — виртуализирован. Признак «Атака» добавлен в данные: keyword id 71 (`mockKeywords`), 21 атакующая карточка каталога помечена 71 точечным патчем (`mockDevelopmentImport`; перегенерация невозможна — ручные код-фиксы не в генераторе). Замеры CDP: переключение вкладки 2.6с → 0.43с; в DOM 23 панели вместо 196; «Атаки»=21, «Физическое развитие»=17, «Приобретённые» показывает купленные+дары; раскрытие панели переживает скролл; RuleListPanel 354 правила → 31 смонтированная строка. Гейт: vitest 493/493, vue-tsc, lint, vite build ✓.
- 2026-08-09 · **Лёгкая раскрывашка (вариант A) — `ExpandableItem.vue` (Core/UI/Component)**: замена Vuetify-аккордеона на строки без Vuetify — `defineModel` (v-model), слоты `#title`/default, триггер `div role="button"` + Enter/Space, тело на `v-show` + CSS `grid-template-rows 0fr→1fr` (без JS-замеров); тень/бордер открытого состояния внутри компонента. `EditorAbilityRow` переведён на него (внешний контракт не менялся; `click.stop` на кнопках покупки не тогглит раскрытие). Гейт зелёный (vitest 493/493, vue-tsc, lint, build). **Замер скролла не изменился (131 vs 133 мс/шаг)** — CPU-профиль показал, что доминирует монтаж Vuetify-микрокомпонентов строки (v-chip/v-btn), а не аккордеон; реальный рычаг — вариант B (лёгкие чипы/кнопки), отложен.
- 2026-08-09 · **Вариант B — лёгкие чипы/кнопки в строке** (реализован): в `EditorAbilityRow` все Vuetify-микрокомпоненты строки заменены на обычные элементы со scoped-стилями — `v-chip`→`<span class="ability-chip">` (tonal/outlined/цвета темой), `v-btn`→`<button class="ability-btn">` (outlined small, без ripple, `:disabled`/`--active`), `v-icon`→`<i class="mdi …">`, `v-text-field`(домен)→`<input class="ability-row__domain-input">`+label. Новых Core/UI-компонентов НЕ создавали (осознанно: параллельный мини-Chip/Btn — вторая система компонентов без нужды; рычаг только в горячей строке). Замеры CDP: скролл avg 131→**62 мс/шаг**, long tasks за 20-шаговый прогон 15→**2**; в строках больше нет `.v-chip`/`.v-btn`/`.v-expansion-panel` (0 аккордеонов), узлы страницы ~940. Функционально: покупка/снятие уровня, чипы (уровень/тип/потрачено), `click.stop` (покупка не тогглит раскрытие), домен-инпут сохраняет значение. Гейт: vitest 493/493, vue-tsc, lint, vite build ✓. Визуал — на глаз пользователю.
- 2026-08-09 · **Light-компоненты в Core/UI + финальная настройка перфа**: чипы/кнопки/поле вынесены в `Core/UI/Component/light/` — `LightChip.vue` (variant tonal/outlined, color primary/secondary), `LightButton.vue` (disabled/active, `$attrs` на нативный button), `LightTextField.vue` (v-model + label). `EditorAbilityRow` переведён на них (локальные стили удалены; через `EditorAbilityPanels` легчают BaseTab/PersonalityTab). VirtualList: `useAnimationFrameWithResizeObserver: true` (батч замеров в rAF), `contain: layout paint/style` на `.virtual-viewport`/`.virtual-item`. **v-memo откачен** (ручной список зависимостей — футган на будущую логику способностей: параметры/механики могут менять рендер без изменения level/met, даст устаревший UI; выгода маргинальна — 1 long task/~5 мс). **Overscan оставлен 8** (буфер против белых полос при быстром скролле; строки дешёвые, разница с 5 не измерима). Финальные замеры: long tasks 4×51–60 мс (на границе порога, headless без GPU — пессимистично), avg ~76 мс/шаг (из них ~50 мс — ожидание 3 rAF). Гейт: vitest 493/493, vue-tsc, lint, vite build ✓.- 2026-08-10 · **Дерево улучшений во вкладке «Развитие»** (D114): у всех ~80 карточек «Улучшение» мока проставлен `parent_ability_code` (родитель = ближайшая предшествующая способность по `catalog.md`; цепочки «улучшение улучшения» — Акробатика → Боевая акробатика → Смертоносные трюки → −2). `EditorAbility.parentCode` + маппинг в `CharacterEditorService.buildAbilities`; **требование родителя синтезируется автоматически** (`has_ability` уровень 1), объединяется с требованиями уровня — ручное дублирование в карточках убрано. UI: слот `#nested` в `EditorAbilityRow`, рекурсивный `DevelopmentAbilityNode` (вложенные улучшения в теле раскрытого родителя, отступ вглубь), в `DevelopmentTab` дерево `childrenByCode` + верхний уровень без родителя (сироты наверху); поиск по имени улучшения включает цепочку родителей в результат и авто-раскрывает её (оверлей к `openSet`). Тест: parentCode + авто-требование (мет/не-мет по родителю, цепочка). Гейт: vitest 494/494, vue-tsc, lint, vite build ✓.
